const add = (a, b) => a + b
const substruct = (a, b) => a - b
const multiply = (a, b) => a * b
const divide = (a, b) => a / b

module.exports = { add, substruct, multiply, divide }

// another way
//exports.add = (a, b) => a + b

